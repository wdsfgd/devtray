## Agent skills

### Issue tracker

Issues and specs live as markdown files under `.scratch/` in this repo. See `docs/agents/issue-tracker.md`.

### Triage labels

Triage state is recorded as a `Status:` line in markdown files (e.g., `needs-triage`, `ready-for-agent`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context repo domain documentation layout. See `docs/agents/domain.md`.
